import { useQuery } from '@tanstack/react-query';
import { Link, useParams } from 'react-router-dom';
import { ArrowRight } from 'lucide-react';
import { api } from '../shared/api/client';
import type { ShippingMethod } from '../shared/types';
import { Breadcrumbs, EmptyState, ErrorState, Loading, Price } from '../shared/ui';

const titles: Record<string, string> = { about: 'Знакомство с FORMA', delivery: 'Доставка и оплата', returns: 'Возврат и отмена', sizes: 'Как выбрать размер', privacy: 'Конфиденциальность' };

export default function InfoPage() {
  const { page = '' } = useParams();
  if (!titles[page]) return <div className="container"><EmptyState title="Страница не найдена" description="Вернитесь в каталог, чтобы продолжить покупки." /></div>;
  return <div className="container page-space"><Breadcrumbs items={[{ label: titles[page] }]} /><div className="page-title-row"><h1>{titles[page]}<span className="heading-dot">.</span></h1></div><div className="info-layout"><nav className="info-nav" aria-label="Информация магазина">{Object.entries(titles).map(([key, title]) => <Link key={key} className={key === page ? 'selected' : ''} to={`/info/${key}`}>{title}</Link>)}</nav><article className="info-content">
    {page === 'about' && <><span className="eyebrow muted">ОДЕЖДА В ТВОЁМ РИТМЕ</span><h2>Меньше лишнего. Больше тебя.</h2><p>FORMA — гардероб для обычной жизни: работы, прогулок, встреч и тех дней, когда никуда не нужно спешить. В центре коллекции — спокойные оттенки, понятные силуэты и вещи, которые можно носить по-своему.</p><p>Собирайте сочетания из футболок, рубашек, трикотажа и верхней одежды. Состав, рекомендации по уходу и доступные размеры указаны в карточке каждой вещи.</p><Link className="button" to="/catalog">Найти свою вещь<ArrowRight size={18} /></Link></>}
    {page === 'delivery' && <Delivery />}
    {page === 'returns' && <><h2>Изменились планы?</h2><p>Откройте историю заказов в личном кабинете, нажмите «Подробнее», затем «Отменить заказ». Отмена доступна, пока заказ ожидает подтверждения или находится на сборке.</p><p>После отмены статус изменится, а состав и стоимость заказа останутся в истории. Повторное оформление создаст новый заказ по актуальным ценам и наличию.</p><h2>После отправки и получения</h2><p>Автоматическая отмена отправленного заказа недоступна. Для возврата после получения понадобится обращение к продавцу. В текущей версии магазина приём обращений и оформление возвратной доставки не подключены.</p><p>Перед началом реальных продаж продавец должен опубликовать свои контакты, реквизиты и условия возврата. Предварительного списания средств в магазине нет.</p><Link className="button secondary" to="/profile/orders">Открыть мои заказы<ArrowRight size={18} /></Link></>}
    {page === 'sizes' && <><h2>Начните со своих мерок</h2><p>Измерьте обхват груди по самой широкой части, талию — в самом узком месте, бёдра — по выступающим точкам. Держите сантиметровую ленту горизонтально, не затягивая её.</p><div className="size-guide"><div><span>01</span><h3>Сравните с любимой вещью</h3><p>Разложите похожую одежду, которая хорошо сидит. Учитывайте её ширину, длину и то, насколько свободной должна быть посадка.</p></div><div><span>02</span><h3>Проверьте размер и цвет</h3><p>На странице товара размеры показаны отдельно для каждого цвета. Недоступные варианты нельзя выбрать.</p></div><div><span>03</span><h3>Учитывайте крой</h3><p>Свободная посадка уже заложена в модели oversize. Не выбирайте больший размер автоматически.</p></div></div><p className="soft-panel">Обозначения S, M и L зависят от модели. Точная таблица мерок производителя пока не предоставлена; универсальную таблицу для всей коллекции мы не используем.</p><Link className="text-link" to="/catalog">Вернуться к коллекции<ArrowRight size={18} /></Link></>}
    {page === 'privacy' && <><h2>Какие данные использует магазин</h2><p>Для аккаунта сохраняются имя, email и хеш пароля. Телефон и адрес нужны для оформления заказа. История содержит состав покупки, цену на момент оформления и адрес доставки.</p><h2>Вход и сохранённые товары</h2><p>Для сохранения входа используется защищённый от доступа JavaScript cookie. Корзина и избранное гостя сохраняются в этом браузере. После входа доступные товары переносятся в аккаунт.</p><h2>Доступ к данным</h2><p>Покупатель видит свои данные и заказы. Администратор магазина управляет каталогом, пользователями и заказами. Данные банковской карты не запрашиваются: доступна оплата при получении.</p><h2>Перед использованием для реальных продаж</h2><p>Оператор персональных данных, контакты для обращений, сроки хранения и юридические условия должны быть указаны владельцем магазина. Эта страница описывает работу текущего приложения и не заменяет политику оператора.</p></>}
  </article></div></div>;
}

function Delivery() {
  const methods = useQuery({ queryKey: ['shipping'], queryFn: () => api<ShippingMethod[]>('/shipping-methods') });
  return <><h2>Выберите удобную доставку</h2><p>Доступные способы доставки показаны ниже. Окончательную сумму товаров и доставки вы увидите перед подтверждением заказа.</p>{methods.isLoading ? <Loading /> : methods.error ? <ErrorState error={methods.error} retry={() => methods.refetch()} /> : methods.data?.length ? <div className="delivery-methods">{methods.data.map(method => <section key={method.id}><div><h3>{method.name}</h3><p>{method.description}</p><span className="muted">{method.estimatedDays}</span></div><Price value={method.price} /></section>)}</div> : <p>Сейчас нет доступных способов доставки.</p>}<h2>Оплата при получении</h2><p>При оформлении заказа деньги не списываются. Заказ сохраняется со статусом «Ожидает подтверждения». Уведомления о его изменениях доступны в истории заказов в личном кабинете.</p><h2>Проверьте адрес</h2><p>Укажите имя получателя, телефон, город, улицу, дом и квартиру. Для следующих покупок адрес можно сохранить в личном кабинете.</p></>;
}
