import type { FieldErrors, UseFormRegister } from 'react-hook-form';
import type { Address } from '../shared/types';
import { Field } from '../shared/ui';
export default function AddressFields({register,errors}:{register:UseFormRegister<Address>;errors:FieldErrors<Address>}){return <div className="address-fields"><Field label="Имя получателя" error={errors.name?.message}><input autoComplete="name" placeholder="Имя и фамилия" {...register('name')}/></Field><Field label="Телефон" error={errors.phone?.message}><input type="tel" autoComplete="tel" placeholder="+7 777 123 45 67" {...register('phone')}/></Field><Field label="Город" error={errors.city?.message}><input autoComplete="address-level2" placeholder="Алматы" {...register('city')}/></Field><Field label="Почтовый индекс (необязательно)" error={errors.postalCode?.message}><input autoComplete="postal-code" placeholder="050000" {...register('postalCode')}/></Field><div className="full-width"><Field label="Улица, дом, квартира" error={errors.street?.message}><input autoComplete="street-address" placeholder="Улица Абая, дом 10, квартира 15" {...register('street')}/></Field></div></div>;}
